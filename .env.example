# Copy to .env (gitignored) and fill in. Sourced by run.sh.
DISCORD_WEBHOOK_URL=
# Optional: prepend a dir to PATH so launchd/systemd/cron can find node
# (e.g. an nvm install). Leave blank if node is already on PATH.
NODE_BIN_PATH=
